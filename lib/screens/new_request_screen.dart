import 'package:flutter/material.dart';

class NewRequest extends StatelessWidget {
  final entertainerId;

  const NewRequest(this.entertainerId);

  static const routeName = '/new-request';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
